spring:
  application:
    name: My spring-boot project
  messages:
    basename: i18n
  thymeleaf:
    enabled: true
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
server:
  port: 8080
